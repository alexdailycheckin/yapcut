{
  "_README": "Per-creator brand config. The skill writes this on first run by interviewing the creator (see SKILL.md 'First run'). Place the real file at <footage>/.yap_build/brand-config.json or the skill root. yapfull.sh reads it.",
  "_fields": {
    "creator": "name, for reference",
    "niche": "one line: what the channel is about (drives hook/story framing)",
    "pillars": "2-4 content pillars",
    "accounts": "handles/accounts to reference or tag",
    "caption_font": "installed font family for word captions (e.g. 'Montserrat Black' default, or a brand display font like 'Bricolage Grotesque ExtraBold')",
    "label_font": "font for the handle/contact block (often a mono, e.g. 'Space Mono')",
    "caption_case": "'on' = ALL CAPS, 'off' = sentence case",
    "accent_hex": "single spark/highlight colour, e.g. '#FF5A2A', or 'none'",
    "base_hex": "caption fill colour (usually white/bone)",
    "ink_hex": "stroke/shadow colour (usually near-black)",
    "handle": "on-screen handle at the CTA, e.g. 'YOURNAME.COM'",
    "contact_lines": "extra lines under the handle, e.g. email + site",
    "cta_clip": "OPTIONAL absolute path to a reusable spoken-outro clip; if set, add it as the final clause in each clauses.json",
    "hook_display_font": "OPTIONAL setup face for the 'branded' hook style (hook_styles.py). Default 'Anton' (free, condensed). Thicker options if you have them: 'Gotham Black', 'Montserrat Black', 'Impact'",
    "hook_anim": "'none' (default: the full hook is on screen at frame zero, which the muted feed judges) or 'typewriter' (a second line after | types in from 1.0s; the first line stays static)",
    "hook_style": "'outline' = heavy stroked hook; 'minimal' = no outline, soft shadow, smaller second line",
    "platforms": "per-platform length band and hook animation; YAP_PLATFORM=<name> re-composes from the cut for that platform",
    "series": "the franchise identity: name (the cover kicker), question_template, mark_png (drawn on covers only when the file exists), pillars"
  },
  "creator": "Your Name",
  "niche": "one line: what your channel is about",
  "pillars": [
    "content pillar 1",
    "content pillar 2",
    "content pillar 3"
  ],
  "accounts": [
    "yoursite.com"
  ],
  "caption_font": "Montserrat Black",
  "label_font": "Space Mono",
  "caption_case": "off",
  "accent_hex": "#FF5A2A",
  "base_hex": "#FFFFFB",
  "ink_hex": "#232323",
  "handle": "YOURNAME.COM",
  "contact_lines": [
    "you@yoursite.com",
    "yoursite.com"
  ],
  "cta_clip": "",
  "hook_anim": "none",
  "hook_style": "outline",
  "platforms": {
    "tiktok": {
      "len": [
        25,
        40
      ],
      "hook_anim": "none"
    },
    "reels": {
      "len": [
        25,
        40
      ]
    },
    "shorts": {
      "len": [
        25,
        60
      ]
    },
    "linkedin": {
      "len": [
        30,
        60
      ],
      "hook_anim": "none",
      "static_first_frame": true
    }
  },
  "series": {
    "name": "",
    "question_template": "How does {subject} sell?",
    "mark_png": "",
    "pillars": [
      "content pillar 1",
      "content pillar 2",
      "content pillar 3"
    ]
  },
  "pip_strict": false
}
